// implement of cycle breaking

#include "cycle_breaking.h"
#include <vector>

// constructor
CycleBreaking::CycleBreaking() {}

// cycle breaking
vector<vector<int>>
CycleBreaking::break_cycle(int vertex_num, vector<vector<int>>& edges) {
  // initialize hash table and max heap
  // find maximum spanning tree without cycle
}
